/* Styling für das Label (über den Feldern) */
label {
    font-size: 16px;         /* Schriftgröße */
    font-weight: bold;       /* Fettgedruckte Schrift */
    display: block;          /* Block-Level-Element, damit es über dem Eingabefeld erscheint */
    margin-bottom: 5px;      /* Abstand unter dem Label */
    color: #333;             /* Textfarbe */
}

/* Optional: Styling für das Eingabefeld */
input {
    padding: 8px;
    margin-bottom: 10px;    /* Abstand nach unten zum nächsten Element */
    width: 100%;            /* Vollständige Breite */
    box-sizing: border-box; /* Verhindert, dass Padding und Border die Gesamtbreite beeinflussen */
}

/* Optional: Styling für das Dropdown-Menü */
select {
    padding: 8px;
    margin-bottom: 10px;    /* Abstand nach unten zum nächsten Element */
    width: 100%;            /* Vollständige Breite */
    box-sizing: border-box; /* Verhindert, dass Padding und Border die Gesamtbreite beeinflussen */
}
